# GiroFacile V89.6.6

Rifinitura Registro Targa senza modifiche al backend/provider:
- badge “Dati verificati” con indicazione del provider;
- evidenza visiva dei campi compilati automaticamente;
- evidenza discreta dei campi non restituiti dal provider, da completare manualmente;
- ripristino corretto degli indicatori quando si cambia targa, si apre un mezzo esistente o si crea un nuovo mezzo.
